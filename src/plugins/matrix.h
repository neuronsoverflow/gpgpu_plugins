#ifndef "MATRIX_H"
#define "MATRIX_H"

// name it matrixMultKernel or matrixMulCuda
// A, B, C must be device pointers
// computes C = A * B
// void call (int numBlocks, int numThreads, Matrix* A, Matrix* B, Matrix* C);

#endif
